package Services;

import java.util.Optional;

public class Check {
  public static Optional<String> verify(String username, String pass) {
    //search for email in file csv and use verifypassword to get correct answer
    return Optional.empty();
  }
}
